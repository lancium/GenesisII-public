package edu.virginia.vcgr.genii.client.utils.dialog;

/**
 * A special type of widget that doesn't echo the answer typed in by the
 * user (and dissallows the setting of default answers).
 * 
 * @author mmm2a
 */
public interface PasswordWidget extends GenericQuestionWidget
{
}