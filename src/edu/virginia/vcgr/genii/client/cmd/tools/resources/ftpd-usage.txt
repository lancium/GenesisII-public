ftpd [options] <port> [network-constraints ...]
	WHERE options are:
		--block
		--idle-timeout=<secs>
		--data-connection-timeout=<secs>
		--max-auth-attempts=<number>
		--sandbox=<path>
		
OR

ftpd stop
