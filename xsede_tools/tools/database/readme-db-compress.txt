


Steps:

+ Download lastest ij tools http://db.apache.org/derby/derby_downloads.html and unzip/untar it.

+ export DERBY_HOME to point to the ij tools directory from previous step.

+ export GENII_USER_DIR for the container which is being compressed.

+ Make sure container is stopped.

+ bash compress-derby-db.sh

