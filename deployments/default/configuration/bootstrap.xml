<?xml version="1.0" encoding="utf-8" ?>
<gsh:script
	xmlns:gsh="http://vcgr.cs.virginia.edu/genii/xsh/script"
	xmlns:geniix="http://vcgr.cs.virginia.edu/genii/xsh/grid">

	<gsh:define name="BOOT_SRVS"
		source="/containers/BootstrapContainer/Services"/>
	
	<gsh:default name="GENII_INSTALL_DIR" value="."/>
	<gsh:default name="DEPLOYMENT_NAME" value="default"/>
	<gsh:default name="SECURITY_DIR" value="${DEPLOYMENT_NAME}/security"/>
	<gsh:default name="PORT" value="18080"/>
	
	<gsh:echo
		message="GENII_INSTALL_DIR is ${GENII_INSTALL_DIR}"/>
	<gsh:echo
		message="DEPLOYMENT_NAME is ${DEPLOYMENT_NAME}"/>

	<gsh:echo
		message="Setting user's current deployment to ${DEPLOYMENT_NAME}."/>
	<geniix:set-user-config>
		<gsh:param>${DEPLOYMENT_NAME}</gsh:param>
	</geniix:set-user-config>
	
	<gsh:echo
		message="Logging in as ${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/keys.pfx."/>
	<geniix:keystoreLogin>
		<gsh:param>--no-gui</gsh:param>
		<gsh:param>--password=keys</gsh:param>
		<gsh:param>--pattern=skynet</gsh:param>
		<gsh:param>--storetype=PKCS12</gsh:param>
		<gsh:param>local:${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/keys.pfx</gsh:param>
	</geniix:keystoreLogin>
	
	<gsh:echo
		message="Creating Root of RNS space."/>
	<geniix:create-rns-root>
		<gsh:param>--protocol=https</gsh:param>
		<gsh:param>--host=localhost</gsh:param>
		<gsh:param>--port=${PORT}</gsh:param>
		<gsh:param>context.xml</gsh:param>
	</geniix:create-rns-root>

	<geniix:mkdir>
		<gsh:param>/containers</gsh:param>
	</geniix:mkdir>

	<geniix:mkdir>
		<gsh:param>/uninitialized-containers</gsh:param>
	</geniix:mkdir>
	
	<geniix:ln>
		<gsh:param>--service-url=https://localhost:${PORT}/axis/services/VCGRContainerPortType</gsh:param>
		<gsh:param>/containers/BootstrapContainer</gsh:param>
	</geniix:ln>

	<gsh:echo
		message="Setting permissions for bootstrap directories and services"></gsh:echo>
	<geniix:chmod>
		<gsh:param>/</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<geniix:chmod>
		<gsh:param>/containers</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<gsh:echo message="Forming general directory structure."/>
	<geniix:mkdir>
		<gsh:param>/bes-containers</gsh:param>
		<gsh:param>/users</gsh:param>
		<gsh:param>/groups</gsh:param>
		<gsh:param>/home</gsh:param>
		<gsh:param>/etc</gsh:param>
		<gsh:param>/queues</gsh:param>
	</geniix:mkdir>

	<geniix:chmod>
		<gsh:param>/bes-containers</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<geniix:chmod>
		<gsh:param>/users</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<geniix:chmod>
		<gsh:param>/groups</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<geniix:chmod>
		<gsh:param>/home</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<geniix:chmod>
		<gsh:param>/etc</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<geniix:chmod>
		<gsh:param>/queues</gsh:param>
		<gsh:param>+r</gsh:param>
		<gsh:param>--everyone</gsh:param>
	</geniix:chmod>

	<gsh:echo message="Adding groups"/>
	<gsh:foreach param-name="FILE" source-dir="${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/certs/groups"
		filter="^.*\.cer$">

		<gsh:define name="GROUP" source="${FILE}"
			pattern="\.cer" replacement="" global="true"/>
		
		<gsh:echo message="Adding group ${GROUP}"/>
		<geniix:cp>
			<gsh:param>--local-src</gsh:param>
			<gsh:param>${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/certs/groups/${FILE}</gsh:param>
			<gsh:param>/groups/${GROUP}</gsh:param>
		</geniix:cp>

		<geniix:chmod>
			<gsh:param>/groups/${GROUP}</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>

		<gsh:echo message="Adding group BootstrapX509AuthN"/>
		<geniix:cp>
			<gsh:param>--local-src</gsh:param>
			<gsh:param>${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/certs/groups/${FILE}</gsh:param>
			<gsh:param>/groups/${GROUP}</gsh:param>
		</geniix:cp>

		<geniix:chmod>
			<gsh:param>/groups/${GROUP}</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>
	</gsh:foreach>

	<gsh:echo message="Adding uva-idp-group"/>
	<geniix:idp>
		<gsh:param>--validDuration=10years</gsh:param>
		<gsh:param>/containers/BootstrapContainer/Services/X509AuthnPortType</gsh:param>
		<gsh:param>uva-idp-group</gsh:param>
	</geniix:idp>
	<geniix:ln>
		<gsh:param>/containers/BootstrapContainer/Services/X509AuthnPortType/uva-idp-group</gsh:param>
		<gsh:param>/groups/uva-idp-group</gsh:param>
	</geniix:ln>

	<gsh:echo message="Adding users"/>
	<gsh:foreach param-name="FILE" source-dir="${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/certs/users"
		filter="^.*\.cer$">
		
		<gsh:define name="USER" source="${FILE}"
			pattern="\.cer" replacement="" global="true"/>
		
		<gsh:echo message="Adding user ${USER}"/>
		<geniix:cp>
			<gsh:param>--local-src</gsh:param>
			<gsh:param>${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/certs/users/${FILE}</gsh:param>
			<gsh:param>/users/${USER}</gsh:param>
		</geniix:cp>

		<geniix:chmod>
			<gsh:param>/users/${USER}</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>
		
		<geniix:mkdir>
			<gsh:param>/home/${USER}</gsh:param>
		</geniix:mkdir>
		
		<geniix:chmod>
			<gsh:param>/home/${USER}</gsh:param>
			<gsh:param>+r+w+x</gsh:param>
			<gsh:param>/users/${USER}</gsh:param>
		</geniix:chmod>
		
		<geniix:chmod>
			<gsh:param>/home/${USER}</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>
	</gsh:foreach>
	
	<gsh:echo message="Adding machines"/>
	<gsh:foreach param-name="URL" source-file="machines">

		<gsh:define name="MACHINE" source="${URL}"
			pattern="^https?://" replacement="" global="true"/>
		<gsh:define name="MACHINE" source="${MACHINE}"
			pattern=":.+$" replacement="" global="true"/>

		<gsh:echo message="Adding machine ${MACHINE}"/>
		<geniix:attach-host>
			<gsh:param>${URL}/axis/services/VCGRContainerPortType</gsh:param>
			<gsh:param>/containers/${MACHINE}</gsh:param>
		</geniix:attach-host>
	</gsh:foreach>

	<gsh:echo message="Setting security on containers/services"/>	
	<gsh:foreach param-name="CONTAINER" source-rns="/containers">
		<gsh:echo message="Setting security on container ${CONTAINER}"/>	
		<geniix:chmod>
			<gsh:param>/containers/${CONTAINER}</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>

		<geniix:chmod>
			<gsh:param>/containers/${CONTAINER}/Services</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>

		<gsh:foreach param-name="SERVICE"
			source-rns="/containers/${CONTAINER}/Services">
			<gsh:echo message="Setting security on ${SERVICE} in container ${CONTAINER}"/>
			<geniix:chmod>
				<gsh:param>/containers/${CONTAINER}/Services/${SERVICE}</gsh:param>
				<gsh:param>+r</gsh:param>
				<gsh:param>--everyone</gsh:param>
			</geniix:chmod>

			<gsh:foreach param-name="GROUP" source-rns="/groups">
				<geniix:chmod>
					<gsh:param>/containers/${CONTAINER}/Services/${SERVICE}</gsh:param>
					<gsh:param>+r+x</gsh:param>
					<gsh:param>/groups/${GROUP}</gsh:param>
				</geniix:chmod>
			</gsh:foreach>
		</gsh:foreach>

		<gsh:echo message="Creating BES resource for ${CONTAINER}."/>
		<geniix:create-resource>
			<gsh:param>/containers/${CONTAINER}/Services/GeniiBESPortType</gsh:param>
			<gsh:param>/bes-containers/${CONTAINER}</gsh:param>
		</geniix:create-resource>

		<geniix:chmod>
			<gsh:param>/bes-containers/${CONTAINER}</gsh:param>
			<gsh:param>+r</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>

		<geniix:chmod>
			<gsh:param>/containers/${CONTAINER}/Services/EnhancedNotificationBrokerFactoryPortType</gsh:param>
			<gsh:param>+x</gsh:param>
			<gsh:param>--everyone</gsh:param>
		</geniix:chmod>

		<gsh:foreach param-name="GROUP" source-rns="/groups">
			<geniix:chmod>
				<gsh:param>/bes-containers/${CONTAINER}</gsh:param>
				<gsh:param>+r+x</gsh:param>
				<gsh:param>/groups/${GROUP}</gsh:param>
			</geniix:chmod>
		</gsh:foreach>
	</gsh:foreach>

	<gsh:echo message="Creating Certificate Generator for containers using keys.pfx."/>
	<geniix:cert-generator>
		<gsh:param>--create-generator</gsh:param>
		<gsh:param>${BOOT_SRVS}/CertGeneratorPortType</gsh:param>
		<gsh:param>--ks-path=${GENII_INSTALL_DIR}/deployments/${SECURITY_DIR}/keys.pfx</gsh:param>
		<gsh:param>--ks-pword=keys</gsh:param>
		<gsh:param>--ks-alias=VCGR Container</gsh:param>
		<gsh:param>/etc/ContainerGroupCertGenerator</gsh:param>
	</geniix:cert-generator>

	<gsh:echo message="Logging out"/>
	<geniix:logout>
		<gsh:param>--all</gsh:param>
	</geniix:logout>

</gsh:script>
